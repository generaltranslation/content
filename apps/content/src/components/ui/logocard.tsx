export function LogoCard({ children }: { children?: React.ReactNode }) {
  return <div>{children}</div>;
}

export function LogoCardContainer({
  children,
}: {
  children?: React.ReactNode;
}) {
  return <div>{children}</div>;
}

export function LogoCardContent({ children }: { children?: React.ReactNode }) {
  return <div>{children}</div>;
}

export function LogoCardImage() {
  return null;
}

export function AllLogoCards() {
  return null;
}
