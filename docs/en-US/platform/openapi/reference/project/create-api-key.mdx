---

title: "Create a Project API Key"
description: "Create an API key for the Project selected by the projectId path parameter. An optional gt-project-id header must match the path. Requires project:api_keys:write, so Project keys cannot create other keys. Select permissions to limit the key, or omit them to delegate all grantable Project permissions held by the request identity. Every selected grant must be available; otherwise the request fails with 403 and no key is created. Save the returned secret securely. Repeated calls create additional keys. API reference for Create a Project API Key."
method: POST
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Create an API key for the Project selected by the projectId path
          parameter. An optional gt-project-id header must match the path.
          Requires project:api_keys:write, so Project keys cannot create other
          keys. Select permissions to limit the key, or omit them to delegate
          all grantable Project permissions held by the request identity. Every
          selected grant must be available; otherwise the request fails with 403
          and no key is created. Save the returned secret securely. Repeated
          calls create additional keys.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<APIPage document={"gt-api"} operations={[{"path":"/v2/projects/{projectId}/api-keys","method":"post"}]} />