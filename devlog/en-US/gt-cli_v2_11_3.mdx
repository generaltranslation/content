---
title: gt@2.11.3
headline: CDN publishing from upload and save-local
date: 2026-03-19
authors: [ernest]
tags: ['gt-cli', 'cdn', 'publish', 'upload', 'save-local']
---

## Overview

[`gt upload`](/docs/cli/reference/commands/upload) and [`gt save-local`](/docs/cli/reference/commands/save-local) now apply configured CDN publishing rules after they sync files. This extends the publishing step already used by [`gt translate`](/docs/cli/reference/commands/translate).

## How it works

- **Upload:** after a successful upload, the CLI publishes or unpublishes files with an explicit publishing configuration.
- **Save local edits:** the CLI runs the publishing step only when it finds and saves local translation changes.
- **Failure handling:** publishing failures produce a warning without failing the completed upload or local-edit sync.

Use the [`publish`](/docs/cli/reference/config#publish) configuration or the `--publish` flag to opt into CDN publishing:

```bash
# Upload files and apply configured CDN publishing rules
npx gt@2.11.3 upload

# Save local edits and publish configured files
npx gt@2.11.3 save-local --publish
```

## Release history

The [2.11.3 changelog](https://github.com/generaltranslation/gt/blob/gt%402.11.3/packages/cli/CHANGELOG.md) records this change in [PR #1133](https://github.com/generaltranslation/gt/pull/1133). Object and array derivation shipped separately in [gt@2.12.0](/devlog/gt-cli_v2_12_0).
